package com.example.tests;

import com.thoughtworks.selenium.*;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import java.util.regex.Pattern;

public class Selenium_Stature_test extends SeleneseTestCase {
	@Before
	public void setUp() throws Exception {
		selenium = new DefaultSelenium("localhost", 4444, "*chrome", "http://rkamv354/");
		selenium.start();
	}

	@Test
	public void testSelenium_Stature_test() throws Exception {
		selenium.open("http://rkamv354/stature");
		selenium.type("USERNAME", "linderp1");
		selenium.type("PASSWORD", "");
		selenium.click("link=Login");
		selenium.waitForPageToLoad("30000");
		selenium.click("css=#prowebLinkId > span");
		selenium.waitForPageToLoad("30000");
	}

	@After
	public void tearDown() throws Exception {
		selenium.stop();
	}
}
